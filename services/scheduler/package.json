{
  "name": "@news/scheduler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file ../../.env wrangler dev src/index.ts --local --port 8788",
    "build": "bun --env-file ../../.env wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@news/env": "workspace:*",
    "@news/platform": "workspace:*",
    "@news/types": "workspace:*",
    "effect": "4.0.0-beta.56"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250418.0",
    "typescript": "5.8.3",
    "wrangler": "^4.12.0"
  }
}
